# Docker Setup

Create a .env.local file

```.env
SOME_VARIABLE=abc
```

Then build the docker file

```powershell
npm run docker-build
```

Run the built docker file

```powershell
npm run docker-run
```